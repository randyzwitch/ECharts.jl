# xy\_plot

`xy_plot` is the internal generic interface that all Cartesian chart types (`bar`, `line`, `area`, `scatter`, etc.) dispatch to. It is exported for advanced use cases.

```@docs
xy_plot
```
